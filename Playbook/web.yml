---
   - hosts: webservers
     remote_user: pierrc
     become: yes

     tasks:

       - name: mise a jour
         apt:
           update_cache: yes
           upgrade: dist
           force_apt_get: yes 

       - name: installation de "Nginx"
         apt:
           name: nginx
           state: present